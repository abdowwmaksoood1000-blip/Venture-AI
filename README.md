open source SaaS you can edit for free on tour own the idea is this an SaaS idea generator SaaS web app that generates SaaS ideas and validate it and you can download the pdf of your result by exporting the pdf by downloading it and you can edit the pricing plans and anything it is full open source


Run Locally
Prerequisites: Node.js

Install dependencies: npm install
Set the GEMINI_API_KEY in .env.local to your Gemini API key
Run the app: npm run dev
